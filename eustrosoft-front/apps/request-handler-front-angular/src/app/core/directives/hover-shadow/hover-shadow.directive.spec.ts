import { HoverShadowDirective } from './hover-shadow.directive';

describe('HoverShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverShadowDirective('' as any);
    expect(directive).toBeTruthy();
  });
});
